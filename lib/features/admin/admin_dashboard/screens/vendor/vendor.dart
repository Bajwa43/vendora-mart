import 'package:flutter/material.dart';

class AdminVendorsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text("🏪 Vendors Overview", style: TextStyle(fontSize: 18)));
  }
}
