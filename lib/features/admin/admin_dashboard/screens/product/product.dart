import 'package:flutter/material.dart';

class AdminProductsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text("📦 Products Catalog", style: TextStyle(fontSize: 18)));
  }
}
